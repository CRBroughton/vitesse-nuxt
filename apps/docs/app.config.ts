import { defineAppConfig } from 'nuxt/app'

export default defineAppConfig({
  ui: {
    colors: {
      primary: 'blue',
      neutral: 'zinc',
      success: 'green',
      info: 'blue',
      warning: 'orange',
      error: 'red',
    },
  },
})
